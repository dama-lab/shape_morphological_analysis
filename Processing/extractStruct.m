function extractStruct(vol,seg,label)
    %% extract structure from targets
    % 	Input:
    % 		- vol: 3D volume of the raw data
    % 		- seg: corresponding segmentation labels
    % 		- labelNo: number of the label 

end